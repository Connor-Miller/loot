[package]
name = "spike-crdt"
version = "0.0.0"
edition.workspace = true
license.workspace = true

[dependencies]
loot-core.workspace = true
thiserror.workspace = true
# TODO: add a CRDT lib (e.g. `automerge` or `yrs`) when fleshing out the spike.
