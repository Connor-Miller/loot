# Agent identity model
GitHub: #58

Type: grilling
Status: open
Blocked by: —

## Question

How do AI agents exist as loot identities? Keypair-per-agent vs one shared agent identity vs keypair-per-dock; how agent keys are provisioned and registered in the peer registry; who grants what to agents at bootstrap; and what content in the loot repo is actually **restricted from agents** for real (the demo needs at least one genuinely sealed path — pick it). Constraint: agents are ephemeral sessions; identity lifecycle must not require per-session ceremony.
